# Contributing

Thanks for helping improve the MakePay Xero connector.

Run `npm run validate` before opening a pull request. Test OAuth, invoice link generation, and payment reconciliation with a Xero demo organisation before release.

Do not commit Xero app secrets, MakePay keys, OAuth refresh tokens, production invoice data, or webhook payloads containing customer information.
